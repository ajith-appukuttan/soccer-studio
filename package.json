{
  "name": "soccer-training-platform",
  "version": "1.0.0",
  "description": "Enterprise Soccer Training Video Editor SaaS Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd packages/shared && npm install && cd ../frontend && npm install && cd ../api-gateway && npm install",
    "build:shared": "cd packages/shared && npm run build",
    "dev:frontend": "cd packages/frontend && npm run dev",
    "dev:api": "cd packages/api-gateway && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "build": "npm run build:shared && cd packages/frontend && npm run build",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist",
    "docker:up": "docker-compose -f infrastructure/docker/docker-compose.yml up",
    "docker:down": "docker-compose -f infrastructure/docker/docker-compose.yml down"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}